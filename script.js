document.addEventListener('DOMContentLoaded',()=>{const slidesContainer=document.querySelector('.slides');const slides=Array.from(slidesContainer.children);const prevButton=document.querySelector('.prev');const nextButton=document.querySelector('.next');const lightbox=document.getElementById('lightbox');const lightboxImg=document.getElementById('lightbox-img');let currentIndex=0;function updateSlide(){slidesContainer.style.transform=`translateX(-${currentIndex*100}%)`}prevButton.addEventListener('click',()=>{currentIndex=currentIndex===0?slides.length-1:currentIndex-1;updateSlide()});nextButton.addEventListener('click',()=>{currentIndex=(currentIndex+1)%slides.length;updateSlide()});slides.forEach(slide=>{slide.addEventListener('click',()=>{if(slide.tagName.toLowerCase()==='video'){const clone=slide.cloneNode(true);clone.controls=true;lightboxImg.replaceWith(clone);clone.id='lightbox-img'}else{lightboxImg.src=slide.src;}lightbox.classList.add('show')})});lightbox.querySelector('.close').addEventListener('click',()=>{lightbox.classList.remove('show');const media=document.getElementById('lightbox-img');if(media.tagName.toLowerCase()==='video'){media.pause();media.currentTime=0;}});});